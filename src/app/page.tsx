import VelorahHome from "@/components/velorah-home";

/*
 * Velorah edition — cinematic one-screen portfolio (video background,
 * glass cards, Instrument Serif). The previous multi-section design
 * (global-background / hero / work / footer) still lives in the repo;
 * to restore it, render those here instead of <VelorahHome />.
 */
export default function Home() {
  return <VelorahHome />;
}
